---
id: T4-C08
tranche: T4
title: Annulations et chaîne de preuves
status: done
priority: must
depends_on: ["T4-C04", "T4-C07"]
prd_refs: ["5:FR-17", "8.3", "10.3", "12", "12.1", "13", "18"]
source_refs: ["REF-PRD", "REF-OAI-CONVERSATIONS", "REF-OAI-CLIENT-EVENTS", "REF-OAI-WEBRTC", "REF-PLAYWRIGHT"]
---

# T4-C08 — Annulations et chaîne de preuves

## 1. Résultat attendu

Prouver de bout en bout que drag, parole, Stop, nouvelle révision et tool call tardif annulent ou invalident correctement pending, réponse et effets, tout en corrélant action, décision, directive, response et evidence IDs.

## 2. Valeur visible pour l'utilisateur ou le jury

L'élève reprend instantanément le contrôle; aucun audio ne chevauche son intervention et aucun ancien conseil ne modifie la figure.

## 3. Prérequis et dépendances

T4-C04 garantit feedback local; T4-C07 complète policy, Realtime et hints. Dépendances : T4-C04 et T4-C07.

## 4. Périmètre inclus

Gestion des annulations T4, journal en mémoire expurgé et scénarios navigateur. `student_drag_started` ou `student_speech_started` invalide tout pending. La parole pendant audio bénéficie de `interrupt_response:true` et de la troncature WebRTC; un Stop applicatif ou une annulation due au drag envoie `response.cancel` puis `output_audio_buffer.clear`. Toute fin/call tardive passe les guards epoch/révision/directive.

## 5. Contrats, types, entrées et sorties

`EvidenceLogEntry = {runId,sequence,at,eventType,epoch,revision,actionId?,decision?,directiveId?,responseId?,callId?,evidenceIds?,outcome,reason}` sans audio, SDP, clé, image ni texte brut sensible. `CancellationReason = student_drag|student_speech|application_stop|stale_revision|reset|response_error`. L'état terminal impose `pendingIntervention:null`; une réponse annulée n'est plus active; les tool calls correspondants sont refusés. La séquence est monotone et l'export reste volontaire, local et expurgé.

## 6. Séquence d'implémentation décidée

Brancher les événements d'activité sur invalidation; normaliser annulation automatique et explicite; envoyer cancel puis clear pour Stop/drag actif; fermer la réponse dans le reducer sur événement terminal; appliquer le guard T4-C05 aux tool calls; écrire les entrées corrélées; construire les scénarios Playwright du golden path et un smoke live opt-in; vérifier absence de données interdites.

## 7. Cas d'erreur et comportements de repli

`response.cancel` sans réponse active ou événement terminal dupliqué reste idempotent. Si `output_audio_buffer.clear` échoue, couper immédiatement l'élément audio local, marquer `clear_failed` et interdire tout nouvel envoi jusqu'à état Realtime cohérent. Un tool call stale reçoit une erreur structurée et ne touche pas GeoGebra. Une déconnexion conserve la construction et le feedback local.

## 8. Critères d'acceptation Given/When/Then

Given une intervention QUEUE, When un drag démarre avant `response.create`, Then pending est nul et aucun audio ne commence. Given le tutor en train de parler, When l'élève parle, Then la réponse devient cancelled et aucun chevauchement tutor n'est audible. Given Stop applicatif, Then `response.cancel` précède `output_audio_buffer.clear`. Given une tool call de révision N après passage à N+1, Then elle est rejetée et le hash GeoGebra reste identique. Given le golden path, Then chaque claim est relié à au moins un evidence ID.

## 9. Tests et commandes de vérification

Exécuté le 14 juillet 2026 sur le candidat courant non commité :

- les tests ciblés C08 passent 107/107;
- `pnpm --dir apps/frontend test --run` passe 383/383 sur 38 fichiers;
- lint, typecheck, build production et `git diff --check` passent;
- `pnpm --dir apps/frontend exec playwright test` passe 13/13 scénarios
  déterministes, dont les cinq scénarios contractuels C08; quatre smokes
  `@live` restent ignorés faute d'opt-in credentialed;
- les cinq scénarios C08 sont des intégrations de contrats dans le runner
  Playwright sans fixture `page`; les huit autres parcours déterministes
  exercent le navigateur réel et les frontières Realtime C08 sont couvertes
  par un fake data channel. Aucun résultat audio live n'est revendiqué.

## 10. Preuves à consigner avant clôture

`CancellationCoordinator` normalise les six raisons, capture pending, réponse,
hint et scope transport, puis terminalise l'état de manière idempotente. Le scope
distingue deux tours successifs au même epoch/révision. Drag, parole, Stop,
reset, nouvelle révision et erreur réponse annulent les effets locaux; Stop et
drag actif envoient `response.cancel` avant `output_audio_buffer.clear`.

Le fake data channel prouve l'annulation d'une réponse pending, active,
proactive ou en tooling, l'ignorance des deltas tardifs et la reprise sur un
nouveau tour. Si le clear échoue, l'audio local est suspendu et tout envoi hors
contrôle de récupération reste bloqué jusqu'à
`output_audio_buffer.cleared`. Une tool call ancienne retourne
`stale_revision` ou `rejected_stale` avant handler; le hash de construction et
le compteur de mutations restent inchangés.

Le journal append-only utilise une allowlist stricte, un `runId` local, une
séquence monotone et un export volontaire en mémoire. Les tests refusent texte
libre et vérifient l'absence d'audio, transcription, image, SDP et clé. La
chaîne action → décision → directive → response → call → evidence IDs
est corrélée. Le golden path confirme `SILENT` sur la première erreur puis
`SPEAK` L1 sans outil sur le blocage répété, avec deux evidence IDs par claim.

La revue distincte C08 et la revue Builder rendent `PASS`. Les modifications
vérifiées depuis la base `fe4be7dbf0d22bf2e9d68b1daff97108fdcc42c8` sont
intégrées au candidat de clôture global T2/T3/T4. Le smoke WebRTC credentialed
avec inspection audio reste la dépendance live explicitement non exécutée.

## 11. Documentation à consulter

[OpenAI Realtime conversations](https://developers.openai.com/api/docs/guides/realtime-conversations), rubrique « Interruption and Truncation », pour barge-in et troncature WebRTC. [Realtime client events](https://developers.openai.com/api/reference/resources/realtime/client-events), entrées `response.cancel` et `output_audio_buffer.clear`, pour l'arrêt applicatif. [Realtime API with WebRTC](https://developers.openai.com/api/docs/guides/realtime-webrtc), rubrique « Sending and receiving events », pour les traces du data channel. [Playwright](https://playwright.dev/docs/intro), rubriques « Running tests » et « Test reports », pour preuves navigateur. [PRD GeoTutor v1.1](/Users/vincentloreaux/Downloads/PRD_GeoTutor_OpenAI_GeoGebra_v1_1.docx), rubriques 8.3, 12.1, 13 et 18, pour les scénarios de clôture.

## 12. Hors périmètre

Reconnexion et fallback de T6, télémétrie distante, stockage persistant, arbitrage complet de toutes les courses T6 et gate live 3/3 final.

## 13. Risques et mitigations

Risque : audio résiduel, événement tardif ou journal sensible. Mitigation : cancel/clear ordonné, guards à chaque frontière, idempotence, coupure audio locale de repli et allowlist des champs journalisés.

## 14. Checklist de clôture Nin-IA

- [x] Implémentation limitée au contrat.
- [x] Tests et scénarios exécutés sur le candidat courant non commité.
- [x] Sorties factuelles et limites consignées.
- [x] Documents pilotes resynchronisés.
