---
id: T3-C01
tranche: T3
title: Versionner l'extraction et le plan canonique
status: done
priority: must
depends_on: ["T0-C06"]
prd_refs: ["5", "6", "9.1", "10.2", "12", "13", "18"]
source_refs: ["REF-OAI-STRUCTURED", "REF-OAI-TERRA", "REF-PRD"]
---

# T3-C01 — Versionner l'extraction et le plan canonique

## 1. Résultat attendu

Définir deux contrats strictement séparés : `ExerciseExtractionWireV1`, produit par le modèle et validé à la frontière, puis `ExercisePlanV1`, dérivé uniquement par du code applicatif. Une sortie modèle ne peut donc jamais choisir de coordonnées, de commande GeoGebra, de permission ou d'objet supplémentaire.

## 2. Valeur visible pour l'utilisateur ou le jury

Une photo reconnue devient une proposition lisible et stable : exercice de médiatrice, points A et B, segment AB, cible laissée à l'élève. Une photo ambiguë demande une précision; un autre exercice est déclaré non supporté sans invention.

## 3. Prérequis et dépendances

T0-C06 close, runtime TypeScript strict disponible et modèle vision fixé à `gpt-5.6-terra`. Dépendance directe : T0-C06. T1-C07 n'est pas requis pour définir les contrats, seulement pour exécuter le plan en T3-C06.

## 4. Périmètre inclus

- Schémas Zod versionnés et types TypeScript inférés.
- Conversion Zod vers JSON Schema compatible Structured Outputs.
- Validation sémantique post-parse et fonction pure `deriveExercisePlanV1`.
- Seul template prêt : médiatrice de `[AB]` avec labels explicites A et B.
- Tests de compatibilité du schéma et de dérivation déterministe.

## 5. Contrats, types, entrées et sorties

`ExerciseExtractionWireV1` est un objet fermé, sans champ optionnel et avec `additionalProperties:false`. Il contient : `schemaVersion:"exercise_extraction.v1"`, `outcome:"ready"|"needs_clarification"|"unsupported"`, `language:"en"|"fr"|"unknown"`, `instruction:string|null`, `pointLabels:string[]`, `segmentEndpoints:[string,string]|null`, `requestedConstruction:"perpendicular_bisector"|"other"|null`, `learningObjective:"perpendicular_bisector_equidistance"|null`, `ambiguityCode:"missing_labels"|"unreadable_text"|"conflicting_instruction"|"missing_segment"|null`, `clarificationQuestion:string|null` et `unsupportedReason:string|null`. Ces trois champs texte restent uniquement dans le wire serveur de compatibilité Structured Outputs; aucun n'est une autorité de présentation.

La validation sémantique exige, pour `ready`, exactement `A` et `B`, le segment `[A,B]`, la cible `perpendicular_bisector` et aucun champ d'ambiguïté. `needs_clarification` exige un code cohérent avec les autres champs; les cas labels étrangers, segment non canonique ou état déjà prêt sont rejetés. `unsupported` exige une construction `other`, sans objectif ni ambiguïté. `ExercisePlanV1` est construit par l'application avec `schemaVersion:"exercise_plan.v1"`, `exerciseId:"demo-perpendicular-bisector-01"`, A `(-3,0)`, B `(3,0)`, AB, `studentMustCreate:["perpendicular_bisector_of_AB"]`, les deux relations cibles `perpendicular` et `passes_through_midpoint`, et `initializationPolicy:"create_givens_only"`.

La frontière client est distincte : `ExerciseReadyClientExtractionV1` ne contient qu'une instruction canonique applicative et les valeurs fermées du template. Les quatre questions viennent exclusivement de `EXERCISE_CLARIFICATION_MESSAGES_V1[ambiguityCode]`; `unsupported` et `refused` utilisent chacun une constante applicative. Le client recalcule ces mêmes vues et ignore les textes HTTP correspondants.

## 6. Séquence d'implémentation décidée

Créer le schéma wire JSON-représentable; générer son JSON Schema; appliquer ensuite une validation sémantique pure; mapper uniquement un résultat `ready` vers le plan canonique; revalider le plan produit; construire ensuite une vue client sans texte modèle libre; dériver clarification et unsupported depuis des tables fermées; exporter des erreurs discriminées et stables; figer des exemples golden sans appeler OpenAI.

## 7. Cas d'erreur et comportements de repli

Labels absents ou illisibles : `needs_clarification`, jamais attribution automatique de A/B. Cible différente : `unsupported`. Champ inattendu, enum inconnu, incohérence entre outcome et champs ou schéma futur : rejet `invalid_extraction`. Aucun rejet ne produit de plan partiel. Nom, adresse, instruction parasite, question ou raison présents dans un texte modèle ne traversent jamais la réponse client : ils sont remplacés par les messages applicatifs. Un refus OpenAI n'est pas forcé dans ce schéma : il est détecté au niveau de la réponse en T3-C04 et rendu génériquement.

## 8. Critères d'acceptation Given/When/Then

- Given une extraction prête décrivant A, B, AB et la médiatrice, When `deriveExercisePlanV1` est appelée deux fois, Then les deux plans sont identiques et ne contiennent que A, B, AB comme givens.
- Given une extraction sans labels, When elle est validée, Then le résultat est `needs_clarification` et aucun plan n'existe.
- Given une construction demandée autre que la médiatrice, When elle est validée, Then le résultat est `unsupported`.
- Given un payload avec une commande ou une propriété supplémentaire, When le schéma le parse, Then il est rejeté.
- Given un nom, une adresse ou une instruction parasite dans n'importe quel texte modèle, When la route construit sa réponse, Then le client reçoit uniquement les textes applicatifs exacts.
- Given chaque `ambiguityCode`, When la clarification est rendue, Then le code produit exactement son message fermé et aucun plan.

## 9. Tests et commandes de vérification

Exécuté le 15 juillet 2026 sur l'état correctif non commité :
`pnpm --dir apps/frontend test --run exercise` passe 11 fichiers et 145/145
tests. `pnpm --dir apps/frontend typecheck` et
`pnpm --dir apps/frontend lint` passent sans erreur ni avertissement.
`pnpm --dir apps/frontend test:e2e --grep "T3 photo"` reconstruit le candidat
Next.js puis passe 5/5 scénarios Playwright. La qualification finale sur commit
figé et ses trois répétitions reste la responsabilité de T3-C08.

## 10. Preuves à consigner avant clôture

Le JSON Schema est généré depuis Zod 4.4.3, conserve les onze champs dans
`required`, ferme chaque objet avec `additionalProperties:false` et représente
`segmentEndpoints` par un tableau homogène `items:{type:"string"}` borné à deux
éléments; ni `$schema` ni `prefixItems` ne traversent la frontière Structured
Outputs. Les fixtures cohérentes, dont un exercice hors MVP portant sur AB,
passent et seize contre-exemples sémantiques sont rejetés avec une raison
stable. Une clarification doit correspondre à son code d'ambiguïté, ne peut
pas être déjà entièrement `ready` et couple strictement l'intention à
l'objectif : `perpendicular_bisector` exige l'objectif canonique, tandis qu'une
intention nulle exige un objectif nul. Un outcome `unsupported` exige au
contraire `requestedConstruction:"other"`, un objectif nul, une raison utile et
aucun champ de clarification; le segment concerné peut légitimement être AB.
Les outcomes sont ainsi mutuellement exclusifs après le contrôle de forme
Structured Outputs. Un `missing_labels` refuse les labels étrangers ou déjà
complets; un `missing_segment` exige A/B mais aucun segment; un
`conflicting_instruction` exige le segment canonique et une intention non
résolue. Le snapshot canonique produit uniquement A `(-3,0)`, B
`(3,0)` et AB, garde la médiatrice dans `studentMustCreate`, fixe les deux
relations cibles et ne contient ni commande ni permission. Deux dérivations
sont égales mais ne partagent pas d'état mutable.

Les tests injectent explicitement `Vincent Loreaux`, `10 Example Street` et une
instruction parasite dans les trois voies ready, clarification et unsupported.
Ni la réponse serveur, ni la normalisation client, ni le parcours Playwright ne
retrouvent ces valeurs. Les quatre codes rendent les quatre messages exacts;
le résultat ready rend l'instruction canonique et les incohérences conservent
`invalid_model_output` sans plan.

## 11. Documentation à consulter

[OpenAI Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs#structured-outputs-vs-json-mode), rubrique « Structured Outputs vs JSON mode »; [refus et entrées incompatibles](https://developers.openai.com/api/docs/guides/structured-outputs#refusals-with-structured-outputs), rubrique « Refusals with Structured Outputs »; [Zod JSON Schema](https://zod.dev/json-schema#ztojsonschema), rubriques « z.toJSONSchema » et « Object schemas »; PRD v1.1, sections 9.1, 10.2 et 13.

## 12. Hors périmètre

Upload, appel Responses, confirmation UI, initialisation GeoGebra, second template, coordonnées extraites de l'image et correction libre du plan.

## 13. Risques et mitigations

Divergence type/JSON Schema : Zod reste la source unique et le schéma généré est testé. Hallucination sémantiquement valide : allowlist MVP et dérivation applicative. Évolution de modèle : `schemaVersion` obligatoire et rejet fermé des versions inconnues.

## 14. Checklist de clôture Nin-IA

- [x] Implémentation limitée au contrat.
- [x] Tests et scénarios exécutés sur l'état candidat.
- [x] Sorties factuelles et limites consignées.
- [x] Documents pilotes resynchronisés.
