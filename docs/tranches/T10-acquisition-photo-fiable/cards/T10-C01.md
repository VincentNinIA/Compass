# T10-C01 — Acquisition photo fiable

## Statut

`done` — `pass` le 15 juillet 2026

## Inclus

- Une entrée fichier sans `capture` pour la galerie et les documents.
- Une entrée caméra distincte avec `accept="image/*"` et
  `capture="environment"` pour privilégier la caméra arrière sur mobile.
- Une validation, un aperçu, un remplacement et un nettoyage communs.
- Le chargement du `.env` racine par `pnpm dev`, sans variable publique.
- Des contrôles EN/FR, clavier, responsive et une lecture réelle credentialed.

## Hors périmètre

- Modification du modèle, du prompt, du schéma ou de l'autorité de confirmation.
- Accès caméra automatique, stockage du média ou persistance de la langue.

## Vérification attendue

```sh
pnpm --dir apps/frontend test --run components/exercise-photo/exercise-uploader.test.tsx
pnpm --dir apps/frontend lint
pnpm --dir apps/frontend typecheck
pnpm --dir apps/frontend test --run
pnpm --dir apps/frontend build
pnpm --dir apps/frontend exec playwright test --grep-invert @live
```

## Preuves obtenues

- Tests ciblés : 36/36; suite complète : 615/615 sur 52 fichiers.
- Lint, typecheck et build passent; Playwright hors live rend 33/33.
- L'eval OpenAI réelle rend 7/7 et la route locale répond `ready` depuis
  `pnpm dev` avec la configuration racine chargée.
- Le navigateur expose `Choisir une photo` sans `capture` et `Prendre une photo`
  avec `accept="image/*"` et `capture="environment"`; la bascule EN/FR fonctionne.
