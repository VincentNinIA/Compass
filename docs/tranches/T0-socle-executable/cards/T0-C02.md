---
id: T0-C02
tranche: T0
title: Initialiser le runtime web
status: done
priority: must
depends_on: ["T0-C01"]
prd_refs: ["11", "12.1", "18"]
source_refs: ["REF-NEXT", "REF-PNPM"]
---

# T0-C02 — Initialiser le runtime web

## 1. Résultat attendu

Créer sous `apps/frontend` une application Next.js App Router TypeScript et un workspace pnpm dont lint, typecheck, tests et build sont reproductibles.

## 2. Valeur visible pour l'utilisateur ou le jury

Un shell GeoTutor local stable devient démontrable.

## 3. Prérequis et dépendances

T0-C01 close; versions Node et pnpm supportées. Dépendances : T0-C01.

## 4. Périmètre inclus

Workspace, frontend, TypeScript strict, scripts, test fumée, `.env.example` et instructions.

## 5. Contrats, types, entrées et sorties

Les commandes racine `pnpm lint|typecheck|test|build` sont l'interface de vérification; aucune clé réelle n'est commise.

## 6. Séquence d'implémentation décidée

Initialiser; épingler versions; configurer scripts; créer page/test; documenter; exécuter les quatre gates.

## 7. Cas d'erreur et comportements de repli

Version incompatible ou dépendance indisponible produit une erreur actionnable, jamais une désactivation de contrôle.

## 8. Critères d'acceptation Given/When/Then

Given un clone propre, When install et les quatre scripts sont lancés, Then ils terminent à zéro et la page répond sans secret.

## 9. Tests et commandes de vérification

À exécuter lors de l'implémentation : `pnpm install --frozen-lockfile`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. Aucun résultat produit n'est revendiqué ici.

## 10. Preuves à consigner avant clôture

- Environnement : Node.js `v22.17.1`, pnpm `10.6.3`, Next.js `16.2.10`.
- `pnpm install --frozen-lockfile` : succès, lockfile à jour.
- `pnpm lint` : succès, ESLint sans diagnostic.
- `pnpm typecheck` : succès, TypeScript sans diagnostic.
- `pnpm test` : succès, 1 fichier et 1 test passés.
- `pnpm build` : succès, route `/` pré-rendue statiquement.
- Smoke Playwright sur le serveur de production : titre `GeoTutor`, shell et statut
  `Runtime ready` visibles, aucune erreur console.
- Capture : `output/playwright/t0-c02-runtime.png`.
- Commit candidat reproductible vérifié :
  `e297a5282ea5d5a9ea8b504a2042820a4b06da90`.

## 11. Documentation à consulter

[Next.js Installation](https://nextjs.org/docs/app/getting-started/installation), rubrique « Automatic installation ». [pnpm Workspaces](https://pnpm.io/workspaces), rubrique « Workspace protocol ».

## 12. Hors périmètre

GeoGebra, OpenAI, audio et logique pédagogique.

## 13. Risques et mitigations

Scaffold implicite; mitigation: versions épinglées et lockfile.

## 14. Checklist de clôture Nin-IA

- [x] Implémentation limitée au contrat.
- [x] Tests et scénarios exécutés sur l'arbre candidat.
- [x] Sorties factuelles et limites consignées.
- [x] Documents pilotes resynchronisés.
