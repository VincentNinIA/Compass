---
id: T2-C06
tranche: T2
title: Annulations vocales
status: done
priority: must
depends_on: ["T2-C05"]
prd_refs: ["FR-03", "FR-17"]
source_refs: ["REF-OAI-CONVERSATIONS", "REF-OAI-CLIENT-EVENTS"]
---

# T2-C06 — Annulations vocales

## 1. Résultat attendu

Gérer barge-in et Stop applicatif par `response.cancel` puis `output_audio_buffer.clear`, avec cleanup local.

## 2. Valeur visible pour l'utilisateur ou le jury

Le tutor vocal reste fluide tout en étant borné par l'état et les preuves de l'application.

## 3. Prérequis et dépendances

T2-C05 closes avec preuves.

## 4. Périmètre inclus

Gérer barge-in et Stop applicatif par `response.cancel` puis `output_audio_buffer.clear`, avec cleanup local. Inclut types, erreurs, budgets et cleanup associés.

## 5. Contrats, types, entrées et sorties

`cancelActiveResponse(reason)` idempotent; invalide response/outils et refuse tout delta tardif.

## 6. Séquence d'implémentation décidée

Suivre réponse/audio; connecter speech_started; envoyer cancel puis clear; arrêter audio local; invalider; rendre idle.

## 7. Cas d'erreur et comportements de repli

Canal fermé ou aucune réponse: opération sûre et aucun audio tardif rendu.

## 8. Critères d'acceptation Given/When/Then

Given audio en cours, When parole ou Stop, Then audio cesse et les deltas de la réponse annulée sont ignorés.

## 9. Tests et commandes de vérification

Remédiation du 15 juillet 2026 : 81/81 tests ciblés couvrent ordre, idempotence,
réponse active, réponse pending avant `response.created`, outil en vol aborté,
rejet du résultat tardif, reprise audio, delta tardif et exception de `send()`.
Cancel/clear réseau sont best-effort; tracks, audio, peer, data channel et état
local sont nettoyés même si l'envoi RTC lève. Le gate final rend lint, typecheck,
81/81 tests ciblés, 420/420 tests frontend partagés et build passants. Le scénario
live Stop passe avec cancel/clear et cleanup; la réserve C01 reste ouverte à cause
des deux autres scénarios non stables du gate global.

## 10. Preuves à consigner avant clôture

Le parcours live corrèle l'identifiant de `response.created` avec
`response.cancel`, observe `output_audio_buffer.started`, puis vérifie que
`response.cancel` précède immédiatement `output_audio_buffer.clear`. La piste,
le data channel, le peer et l'audio local sont ensuite fermés. Les replays
ignorent tout delta portant l'identifiant annulé, annulent aussi le tour pending
ou tooling et ne répètent pas le couple cancel/clear pour un même speech item.

## 11. Documentation à consulter

[Realtime Conversations](https://developers.openai.com/api/docs/guides/realtime-conversations#interruption-and-truncation), « Interruption and truncation ». [Client Events](https://developers.openai.com/api/docs/guides/realtime-client-events), `response.cancel` et `output_audio_buffer.clear`.

## 12. Hors périmètre

Arbitre global T6.

## 13. Risques et mitigations

Audio local persistant; arrêter aussi élément/tracks.

## 14. Checklist de clôture Nin-IA

- [x] Gateway/session limités au contrat.
- [x] Scénarios transport et erreurs rejoués.
- [x] Preuves corrélées et expurgées.
- [x] Pilotes resynchronisés.
