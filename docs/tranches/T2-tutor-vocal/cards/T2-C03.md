---
id: T2-C03
tranche: T2
title: Gateway fermé
status: done
priority: must
depends_on: ["T1-C07", "T2-C01"]
prd_refs: ["FR-07"]
source_refs: ["REF-OAI-FUNCTIONS"]
---

# T2-C03 — Gateway fermé

## 1. Résultat attendu

Autoriser exactement quatre outils stricts avec budgets, révision et idempotence `call_id`.

## 2. Valeur visible pour l'utilisateur ou le jury

Le tutor vocal reste fluide tout en étant borné par l'état et les preuves de l'application.

## 3. Prérequis et dépendances

T1-C07, T2-C01 closes avec preuves.

## 4. Périmètre inclus

Autoriser exactement quatre outils stricts avec budgets, révision et idempotence `call_id`. Inclut types, erreurs, budgets et cleanup associés.

## 5. Contrats, types, entrées et sorties

Allowlist `read_construction|initialize_exercise|check_relation|highlight_objects`; schemas `additionalProperties:false`; enveloppe `{ok,callId,revision,data|error,evidenceIds}`.

## 6. Séquence d'implémentation décidée

Déclarer schemas; parser strict; vérifier nom/phase/révision/budget; mémoriser par call_id; expurger.

## 7. Cas d'erreur et comportements de repli

Inconnu, JSON invalide, stale ou budget dépassé: zéro exécution; duplicate retourne le résultat mémorisé.

## 8. Critères d'acceptation Given/When/Then

Given appels valides/invalides/dupliqués, When gateway, Then seuls quatre contrats valides s'exécutent au plus une fois.

## 9. Tests et commandes de vérification

Requalification du 14 juillet 2026 : la phase n'est plus une constante du
composant. `TutorWorkspace` fournit l'autorité runtime `idle →
exercise_confirmed → constructing`, le plan confirmé courant et la révision
GeoGebra réelle. Les 22/22 tests gateway/handlers/intégration GeoGebra passent,
ainsi que lint, typecheck, 216/216 tests frontend et build. Les protections
historiques — quatre outils, parse fermé, phase, stale, budgets et idempotence —
restent couvertes sans régression.

## 10. Preuves à consigner avant clôture

Les quatre appels conformes sont autorisés; leur contexte capture la phase et la
révision au moment de l'appel. Outil inconnu, JSON invalide,
propriété supplémentaire, TTL hors borne, phase invalide, révision stale et
budget dépassé sont refusés avant handler. Deux appels concurrents de même
`call_id` partagent une promesse et un seul effet; les enveloppes d'erreur ont
une liste `evidenceIds` vide et n'exposent pas l'exception.

## 11. Documentation à consulter

[Function Calling](https://developers.openai.com/api/docs/guides/function-calling), « Defining functions » et « Strict mode ».

## 12. Hors périmètre

Commande GeoGebra générique et accès système.

## 13. Risques et mitigations

Validation partielle; parse strict avant mutation.

## 14. Checklist de clôture Nin-IA

- [x] Gateway/session limités au contrat.
- [x] Scénarios transport et erreurs rejoués.
- [x] Preuves corrélées et expurgées.
- [x] Pilotes resynchronisés.
