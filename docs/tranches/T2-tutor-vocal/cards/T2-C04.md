---
id: T2-C04
tranche: T2
title: Outils cœur
status: done
priority: must
depends_on: ["T2-C03"]
prd_refs: ["FR-02", "FR-07", "FR-08"]
source_refs: ["REF-OAI-FUNCTIONS", "REF-GGB-API"]
---

# T2-C04 — Outils cœur

## 1. Résultat attendu

Implémenter les quatre handlers sur les services déterministes T1, sans construire la solution.

## 2. Valeur visible pour l'utilisateur ou le jury

Le tutor vocal reste fluide tout en étant borné par l'état et les preuves de l'application.

## 3. Prérequis et dépendances

T2-C03 closes avec preuves.

## 4. Périmètre inclus

Implémenter les quatre handlers sur les services déterministes T1, sans construire la solution. Inclut types, erreurs, budgets et cleanup associés.

## 5. Contrats, types, entrées et sorties

Contrats: `read_construction({revision})`, `initialize_exercise({planId,expectedRevision})`, `check_relation({relation,objects,revision})`, `highlight_objects({names,style,ttlMs,revision})`; highlight temporaire.

## 6. Séquence d'implémentation décidée

Lier services; vérifier phase/révision; borner noms/TTL; sérialiser; nettoyer; produire evidence.

## 7. Cas d'erreur et comportements de repli

Objet absent, plan non confirmé ou stale: erreur sans mutation.

## 8. Critères d'acceptation Given/When/Then

Given chaque outil dans/hors phase, When appelé, Then seule l'opération prévue a lieu et lecture/check cite une preuve.

## 9. Tests et commandes de vérification

Requalification du 14 juillet 2026 : `initialize_exercise` ne possède plus de
bootstrap local A(-2)/B(2). Il exige le véritable `ExerciseConfirmedV1`, puis
délègue à `ExerciseInitializationRuntime` T3 et sa transaction rollbackable.
Les 22/22 tests gateway/handlers/intégration GeoGebra passent et prouvent le plan
canonique A(-3,0), B(3,0), AB sans commande historique. Lint, typecheck,
216/216 tests frontend, build et 5/5 smokes T3 passent.

Correctif du 15 juillet 2026 : `check_relation` accepte exclusivement
`[candidate,"AB"]` pour `perpendicular` et `[candidate,"A","B"]` pour
`passes_midpoint`. Arity, ordre, owner/kind et concordance exacte des objets de
preuve sont contrôlés; 27/27 tests gateway/handlers passent.

## 10. Preuves à consigner avant clôture

`read_construction` retourne le snapshot canonique et un ID lié au hash;
`check_relation` ne retourne que la mesure déterministe demandée et son ID.
L'initialisation confirmée crée exactement A/B/AB via la transaction T3, et ses
échecs restaurent le checkpoint exact. Le highlight change seulement la couleur puis restaure
`#112233`; l'inventaire reste identique et aucun objet solution n'est créé.

## 11. Documentation à consulter

[Function Calling](https://developers.openai.com/api/docs/guides/function-calling), « Strict mode ». [Apps API](https://geogebra.github.io/docs/reference/en/GeoGebra_Apps_API/), création/lecture/style.

## 12. Hors périmètre

Construction libre.

## 13. Risques et mitigations

Highlight orphelin; namespace, TTL, cleanup.

## 14. Checklist de clôture Nin-IA

- [x] Gateway/session limités au contrat.
- [x] Scénarios transport et erreurs rejoués.
- [x] Preuves corrélées et expurgées.
- [x] Pilotes resynchronisés.
