---
id: T1-C07
tranche: T1
title: Checkpoint et reset
status: done
priority: must
depends_on: ["T1-C06"]
prd_refs: ["FR-11"]
source_refs: ["REF-GGB-API"]
---

# T1-C07 — Checkpoint et reset

## 1. Résultat attendu

Restaurer exactement le checkpoint initial via `getBase64`/`setBase64`, comparer le hash et réconcilier les listeners.

## 2. Valeur visible pour l'utilisateur ou le jury

La construction devient observable, explicable et vérifiable sans dépendre d'un modèle.

## 3. Prérequis et dépendances

Dépendances : T1-C06. Les preuves de ces cartes doivent être disponibles.

## 4. Périmètre inclus

Restaurer exactement le checkpoint initial via `getBase64`/`setBase64`, comparer le hash et réconcilier les listeners. Sont inclus les types, le cycle de vie, les erreurs et le cleanup nécessaires.

## 5. Contrats, types, entrées et sorties

`Checkpoint{base64,initialHash}` en mémoire; reset exclusif incrémente l'epoch, suspend bridge et invalide les travaux antérieurs.

## 6. Séquence d'implémentation décidée

Capturer après init; annuler; suspendre; restaurer; attendre ready; reconstruire registre/listeners; comparer hash; remettre progrès.

## 7. Cas d'erreur et comportements de repli

Base64 corrompu ou hash divergent: reconstruire A/B/AB depuis fixture canonique et signaler recovery.

## 8. Critères d'acceptation Given/When/Then

Given objets/listeners ajoutés, When Reset, Then le hash initial revient et chaque listener existe une fois.

## 9. Tests et commandes de vérification

À exécuter : tests reset répété/pendant drag et intégration vrai applet round-trip. Puis `pnpm lint`, `pnpm typecheck`, `pnpm test` et, si UI touchée, `pnpm build`. Aucun résultat n'est encore revendiqué.

## 10. Preuves à consigner avant clôture

- Tests : restauration nominale au hash initial; reset concurrent dédupliqué;
  action pending annulée; Base64 divergent reconstruit seulement A/B/AB puis
  retrouve le hash initial.
- Smoke applet réel : avant reset, médiatrice + P et progression 2/2; après reset,
  hash `fnv1a32:08796ade`, objets `[A,AB,B]`, progression 0/2, epoch 2 et quatre
  listeners. Reset répété : même hash, epoch 3 et toujours quatre listeners.
- Exécuté le 14 juillet 2026 : 4 tests C07, 14/14 tests bridge/checkpoint/UI,
  typecheck et lint passants. Capture : `output/playwright/t1-c07-reset-exact.png`.
- Gate final T1 : lint pass; typecheck pass; 11 fichiers/54 tests pass; build
  Next.js optimisé pass.
- Correctif de réouverture : le checkpoint conserve l'inventaire exhaustif initial,
  le compare avant de reconstruire le registre et le revérifie après recovery.
  `setBase64` expire après 3 s. Régressions unitaires et applet réelle : objet
  `rogue` et callback silencieux forcent `recovered:true`, inventaire final
  `[A,AB,B]`, hash initial et quatre listeners. 7/7 tests checkpoint/UI passants.
  Capture : `output/playwright/t1-c07-rogue-recovery.png`.
- Gate de requalification T1 : 39/39 tests ciblés, lint, typecheck, 59/59 tests
  complets et build Next.js optimisé passants.

## 11. Documentation à consulter

[Apps API](https://geogebra.github.io/docs/reference/en/GeoGebra_Apps_API/), « GeoGebra's File format », `getBase64` et `setBase64`.

## 12. Hors périmètre

Persistance disque/IndexedDB.

## 13. Risques et mitigations

Callbacks anciens; epoch et réenregistrement centralisé.

## 14. Checklist de clôture Nin-IA

- [x] Contrat et ownership respectés.
- [x] Tests et scénarios exécutés sur le commit candidat.
- [x] Preuves et limites consignées.
- [x] Pilotes Nin-IA resynchronisés.
