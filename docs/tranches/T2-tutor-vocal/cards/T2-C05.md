---
id: T2-C05
tranche: T2
title: Boucle d'outil Realtime
status: done
priority: must
depends_on: ["T2-C02", "T2-C04"]
prd_refs: ["FR-03", "FR-07"]
source_refs: ["REF-OAI-CONVERSATIONS", "REF-OAI-FUNCTIONS"]
---

# T2-C05 — Boucle d'outil Realtime

## 1. Résultat attendu

Transformer `response.done` en exécution gateway, `function_call_output` corrélé, puis unique `response.create`.

## 2. Valeur visible pour l'utilisateur ou le jury

Le tutor vocal reste fluide tout en étant borné par l'état et les preuves de l'application.

## 3. Prérequis et dépendances

T2-C02, T2-C04 closes avec preuves.

## 4. Périmètre inclus

Transformer `response.done` en exécution gateway, `function_call_output` corrélé, puis unique `response.create`. Inclut types, erreurs, budgets et cleanup associés.

## 5. Contrats, types, entrées et sorties

Exécuter seulement les `function_call` completed; même `call_id`; budget par tour et une continuation après le lot.

## 6. Séquence d'implémentation décidée

Recevoir done; filtrer/dédupliquer; exécuter; publier outputs; demander continuation; fermer cycle.

## 7. Cas d'erreur et comportements de repli

Réponse annulée, arguments incomplets ou timeout: ne pas parler sur preuve absente; output d'erreur si canal ouvert.

## 8. Critères d'acceptation Given/When/Then

Given deux appels completed, When done arrive, Then deux outputs uniques précèdent une continuation.

## 9. Tests et commandes de vérification

Remédiation du 15 juillet 2026 : 81/81 tests ciblés Realtime/gateway/handlers
passent. Un timeout abort le handler, publie un `function_call_output` d'erreur
sûr après achèvement du rollback coopératif et terminalise le cycle sans
continuation; un rejet gateway suit la même
voie. Une réponse non `completed`, un envoi impossible ou un budget épuisé ne
reste jamais en `tooling`. Le gate final rend lint, typecheck, 81/81 tests ciblés,
420/420 tests frontend partagés et build passants. En live, l'output d'outil
corrélé est observé, mais la continuation expire avant le second `response.done`;
la réserve T2-C01 reste donc ouverte et n'est pas présentée comme une réussite.

## 10. Preuves à consigner avant clôture

Le parcours live observe un `read_construction` completed, publie exactement un
`function_call_output` avec le même `call_id`, puis une unique continuation. Les
tests unitaires prouvent que tous les outputs d'un lot précèdent la continuation,
qu'une réponse dupliquée reste sans effet, qu'un timeout est rendu au modèle
comme output corrélé, et qu'un canal fermé termine localement le tour en échec.

## 11. Documentation à consulter

[Realtime Conversations](https://developers.openai.com/api/docs/guides/realtime-conversations#function-calling), « Function calling ». [Function Calling](https://developers.openai.com/api/docs/guides/function-calling), résultats d'outil.

## 12. Hors périmètre

Exécution avant done et parallélisme libre.

## 13. Risques et mitigations

Boucle infinie; budget d'itérations.

## 14. Checklist de clôture Nin-IA

- [x] Gateway/session limités au contrat.
- [x] Scénarios transport et erreurs rejoués.
- [x] Preuves corrélées et expurgées.
- [x] Pilotes resynchronisés.
