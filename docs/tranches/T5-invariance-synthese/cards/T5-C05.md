---
id: T5-C05
tranche: T5
title: Synthèse hors bande
status: done
priority: must
depends_on: ["T5-C04"]
prd_refs: ["FR-12"]
source_refs: ["REF-OAI-CONVERSATIONS"]
---

# T5-C05 — Synthèse hors bande

## 1. Résultat attendu

Générer une synthèse texte via réponse Realtime hors conversation avec `conversation:"none"`, metadata de routage et fallback déterministe.

## 2. Valeur visible pour l'utilisateur ou le jury

L'élève observe cinq mesures, conjecture l'invariance et obtient une synthèse sans perdre sa construction.

## 3. Prérequis et dépendances

T5-C04 closes et prouvées.

## 4. Périmètre inclus

Générer une synthèse texte via réponse Realtime hors conversation avec `conversation:"none"`, metadata de routage et fallback déterministe. Inclut annulation, cleanup et observabilité.

## 5. Contrats, types, entrées et sorties

Request text-only, `tools:[]`, metadata `{kind,runId,revision}`, evidence IDs dans contexte; sortie UI, jamais audio ni conversation par défaut.

Implémenté par `InvarianceOobSummaryCoordinator` : un unique
`response.create` porte un `event_id`, `conversation:"none"`, un contexte
`input` neuf limité aux cinq tuples mesure/preuve, `output_modalities:["text"]`,
`tools:[]`, `tool_choice:"none"` et trois metadata string. Aucun
`conversation.item.create` n'est émis.

## 6. Séquence d'implémentation décidée

Construire prompt borné; créer réponse OOB; router par metadata/response id; valider révision; rendre texte; fallback local si erreur.

## 7. Cas d'erreur et comportements de repli

Timeout, réponse vide, erreur ou stale: ignorer contenu et utiliser résumé déterministe à partir des cinq mesures.

Le même fallback exact couvre aussi send impossible, `response.done` non
`completed`, metadata/output/conversation invalides et fermeture transport. Un
résultat invalide avant la frontière 5/5 reste ignoré; un run en vol ou terminal
est dédupliqué.

## 8. Critères d'acceptation Given/When/Then

Given 5/5, When OOB réussit, Then texte n'entre pas dans conversation; When elle échoue, Then fallback exact est affiché.

## 9. Tests et commandes de vérification

Le 15 juillet 2026 : 16/16 tests unitaires C05 et 42/42 avec l'intégration
WebRTC passent. La suite globale rend 474/474 sur 43 fichiers; lint, typecheck,
build et 14/14 scénarios Playwright hors `@live` passent. Aucun scénario C05
credentialed n'a été exécuté ni revendiqué.

## 10. Preuves à consigner avant clôture

Événements request/done expurgés, metadata, absence d'item conversation et texte/fallback.

Couverture obtenue : contrat OOB fermé, absence de fuite du hash, des deux
preuves d'autorité, du candidat ou d'un transcript; routage concurrent
metadata/response ID, done direct, duplicate, stale avant envoi et avant rendu,
timeout, erreur corrélée, texte vide, audio/conversation forgés et fallback
déterministe exact.

## 11. Documentation à consulter

[Realtime Conversations](https://developers.openai.com/api/docs/guides/realtime-conversations#create-responses-outside-the-default-conversation), « Create responses outside the default conversation ».

Références vérifiées via OpenAI Docs MCP :

- [`response.create`](https://developers.openai.com/api/reference/resources/realtime/client-events#response.create)
- [`response.done`](https://developers.openai.com/api/reference/resources/realtime/server-events#response.done)

## 12. Hors périmètre

Audio et outil durant synthèse.

## 13. Risques et mitigations

Mauvais routage; metadata plus map responseId.

## 14. Checklist de clôture Nin-IA

- [x] Cinq échantillons et preuves complets.
- [x] Rollback/annulation/fallback rejoués.
- [x] Accessibilité préservée : aucune UI C06 ajoutée; Playwright hors live passe.
- [x] Pilotes resynchronisés vers T5-C06.
